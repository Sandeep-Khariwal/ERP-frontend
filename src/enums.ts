export enum UserTypes {
    ADMIN = "admin",
    USER = "user",
    TEACHER = "teacher",
    STUDENT = "student",
    OTHER = "other"
   }

   export enum Tabs {
     DASHBOARD = "dashboard",
     STUDENT = "student",
     TEACHER = "teacher",
     TRANSPORT = "Transport"
   }